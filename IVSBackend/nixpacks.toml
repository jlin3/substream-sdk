# Nixpacks configuration for IVS Streaming Backend
# Root directory should be set to IVSBackend in Railway

[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]  
cmds = ["pnpm db:generate && pnpm build"]

[start]
cmd = "pnpm start"
