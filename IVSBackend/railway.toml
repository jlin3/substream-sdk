# Railway deployment configuration for IVS Streaming Backend
# Set Root Directory to "IVSBackend" in Railway settings

[build]
builder = "nixpacks"
buildCommand = "corepack enable && pnpm install && pnpm db:generate && pnpm build"

[deploy]
startCommand = "corepack enable && pnpm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

