[phases.setup]
nixPkgs = ["python312", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python -c 'print(\"build ok\")'"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080}"
